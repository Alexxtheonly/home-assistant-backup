platform: template
sensors:
 light_toilet_color_temp:
  value_template: "{% if is_state('light.toilett', 'on') %}
          {{ states.light.toilett.attributes.color_temp | int }}
          {% else %}
            n/a
          {% endif %}"