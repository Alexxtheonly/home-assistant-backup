platform: template
sensors:
 awake_motion:
  friendly_name: 'Awake motion'
  entity_id:
   -  group.awake_movement_sensors
  value_template: >
    {{ 
      is_state('sensor.awake_movement', 'on') and
      (as_timestamp(now()) - as_timestamp(states.sensor.awake_movement.last_changed)) >= 600
    }}